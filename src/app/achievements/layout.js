export const metadata = {
    title: 'Achievements - CourtSide',
    description: 'Erhalte Auszeichnungen für deine Erfolge in CourtSide.'
};

export const viewport = {
    width: "device-width",
    initialScale: 1,
    maximumScale: 1
};

export default function AchievementsLayout({ children }) {
    return children;
} 